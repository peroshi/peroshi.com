body
    flexbox()
    flex-direction(column)
    min-height: 100vh

.structure
    padding: 0 15px
    @media ({$break-phone})
        margin: 0 auto
        max-width: 1200px
        padding: 0 40px

#site-content
    flex(1)
